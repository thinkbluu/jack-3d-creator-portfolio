export default function ComparisonSection() {
  /*
   * Comparison section temporarily disabled until real before/after assets exist.
   * The interactive section content will be restored with production imagery.
   */
  return null
}
